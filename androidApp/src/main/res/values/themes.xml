<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Brand colors from app icon -->
    <color name="brand_primary">#FF6370D8</color>
    <color name="brand_primary_light">#FF8E97DF</color>
    <color name="brand_accent">#FFF06236</color>

    <!-- Base application theme (Light) -->
    <style name="Theme.JustFyi" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Splash screen background color (Light) -->
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <!-- Status bar and navigation bar -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Splash screen colors (Light) -->
    <color name="splash_background">#FFFBFBFF</color>
</resources>
